package com.example.memotrip_kroniq.ui.addtrip.utils

enum class AddTripScrollTarget {
    TRIP_NAME,
    HERO_BANNER,
    DESTINATION,
    THEME,
    DATE,
    ROUTE_BLOCK,   // From + Add stop + Stops + To
    TRANSPORT,
    CREATE_BUTTON
}
